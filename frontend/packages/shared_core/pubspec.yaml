name: shared_core
description: Shared domain models and use-cases for Jaspr and Flutter clients.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.8

dependencies:
  http: ^1.5.0
  meta: ^1.17.0
